# Local development keeps this empty and uses the Vite proxy below.
VITE_API_BASE_URL=

# Existing live Local Sewa backend used during local development.
VITE_DEV_API_PROXY=https://localsewa.com

# Used only when a signed-in profile or saved location is not available.
VITE_DEFAULT_LOCATION=Chandrapur
